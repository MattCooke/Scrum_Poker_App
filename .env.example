# Server Configuration
# Hostname/IP address for the dev server (default: localhost)
# Use 0.0.0.0 to allow access from other devices on your network
HOSTNAME=localhost

# Port for the dev server (default: 3000)
PORT=3000
